<!-- DiplomMurtazin/View/DashboardPage.xaml -->
<Page x:Class="DiplomMurtazin.View.DashboardPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
      xmlns:vm="clr-namespace:DiplomMurtazin.ViewModel"
      Title="DashboardPage" Background="#ecf0f1">

    <Page.DataContext>
        <vm:DashboardViewModel/>
    </Page.DataContext>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <!-- Заголовок -->
            <TextBlock Text="Панель управления" FontSize="30" FontWeight="Bold" Margin="0 0 0 20"/>

            <!-- Фильтры -->
            <Border Background="White" CornerRadius="5" Padding="15" Margin="0 0 0 20">
                <StackPanel>
                    <WrapPanel Margin="0 0 0 10">
                        <Button Content="Сегодня" Command="{Binding SetTodayCommand}" Margin="5" Width="80" Height="30"/>
                        <Button Content="Вчера" Command="{Binding SetYesterdayCommand}" Margin="5" Width="80" Height="30"/>
                        <Button Content="Неделя" Command="{Binding SetWeekCommand}" Margin="5" Width="80" Height="30"/>
                        <Button Content="Месяц" Command="{Binding SetMonthCommand}" Margin="5" Width="80" Height="30"/>
                        <Button Content="Год" Command="{Binding SetYearCommand}" Margin="5" Width="80" Height="30"/>
                    </WrapPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <DatePicker Grid.Column="0" SelectedDate="{Binding StartDate}" Margin="5" Height="30"/>
                        <DatePicker Grid.Column="1" SelectedDate="{Binding EndDate}" Margin="5" Height="30"/>
                        <ComboBox Grid.Column="2" ItemsSource="{Binding Cashiers}" SelectedItem="{Binding SelectedCashier}" DisplayMemberPath="FullName" Margin="5" Height="30"/>
                        <Button Grid.Column="3" Content="Применить" Command="{Binding RefreshCommand}" Margin="5" Width="100" Height="30" Background="#3498db" Foreground="White"/>
                    </Grid>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="2*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <ComboBox Grid.Column="0" ItemsSource="{Binding Categories}" SelectedItem="{Binding SelectedCategory}" DisplayMemberPath="CategoryName" Margin="5" Height="30"/>
                        <TextBox Grid.Column="1" Text="{Binding ProductSearch, UpdateSourceTrigger=PropertyChanged}" Margin="5" Height="30" Padding="10,5" ToolTip="Название товара или штрих-код"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Карточки показателей -->
            <WrapPanel Margin="0 0 0 20">
                <Border Width="230" Height="120" Margin="10" CornerRadius="10" Background="#3498db">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Общая выручка" Foreground="White" FontSize="18" HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding TotalRevenue, StringFormat={}{0:F2} ₽}" Foreground="White" FontSize="26" FontWeight="Bold" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
                <Border Width="230" Height="120" Margin="10" CornerRadius="10" Background="#2ecc71">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Количество продаж" Foreground="White" FontSize="18" HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding SalesCount}" Foreground="White" FontSize="26" FontWeight="Bold" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
                <Border Width="230" Height="120" Margin="10" CornerRadius="10" Background="#9b59b6">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Всего товаров" Foreground="White" FontSize="18" HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding ProductsCount}" Foreground="White" FontSize="26" FontWeight="Bold" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
                <Border Width="230" Height="120" Margin="10" CornerRadius="10" Background="#e74c3c">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Мало товара" Foreground="White" FontSize="18" HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding LowStockCount}" Foreground="White" FontSize="26" FontWeight="Bold" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </WrapPanel>

            <!-- График продаж -->
            <Border Background="White" CornerRadius="10" Padding="15" Margin="0 0 0 20">
                <StackPanel>
                    <TextBlock Text="Продажи по дням" FontSize="22" FontWeight="Bold" Margin="0 0 0 15"/>
                    <lvc:CartesianChart Series="{Binding SalesSeries}" Height="300">
                        <lvc:CartesianChart.AxisX>
                            <lvc:Axis Labels="{Binding SalesLabels}"/>
                        </lvc:CartesianChart.AxisX>
                        <lvc:CartesianChart.AxisY>
                            <lvc:Axis LabelFormatter="{Binding Formatter}"/>
                        </lvc:CartesianChart.AxisY>
                    </lvc:CartesianChart>
                </StackPanel>
            </Border>

            <!-- Статистика по кассирам -->
            <Border Background="White" CornerRadius="10" Padding="15" Margin="0 0 0 20">
                <StackPanel>
                    <TextBlock Text="Статистика по кассирам" FontSize="22" FontWeight="Bold" Margin="0 0 0 15"/>
                    <DataGrid ItemsSource="{Binding CashierStats}" AutoGenerateColumns="False" Height="200">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Кассир" Binding="{Binding EmployeeName}" Width="*"/>
                            <DataGridTextColumn Header="Продаж" Binding="{Binding SalesCount}" Width="100"/>
                            <DataGridTextColumn Header="Выручка" Binding="{Binding TotalAmount, StringFormat={}{0:F2} ₽}" Width="120"/>
                            <DataGridTextColumn Header="Средний чек" Binding="{Binding AverageCheck, StringFormat={}{0:F2} ₽}" Width="120"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>

            <!-- Топ-10 товаров -->
            <Border Background="White" CornerRadius="10" Padding="15" Margin="0 0 0 20">
                <StackPanel>
                    <TextBlock Text="Топ-10 товаров" FontSize="22" FontWeight="Bold" Margin="0 0 0 15"/>
                    <DataGrid ItemsSource="{Binding TopProducts}" AutoGenerateColumns="False" Height="300">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Товар" Binding="{Binding ProductName}" Width="*"/>
                            <DataGridTextColumn Header="Продано, шт" Binding="{Binding TotalQuantity}" Width="100"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>

            <!-- Низкие остатки с прогнозом -->
            <Border Background="White" CornerRadius="10" Padding="15" Margin="0 0 0 20">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0 0 0 10">
                        <TextBlock Text="Низкие остатки и прогноз" FontSize="22" FontWeight="Bold" VerticalAlignment="Center"/>
                    </StackPanel>
                    <DataGrid ItemsSource="{Binding LowStockItems}" AutoGenerateColumns="False" Height="300">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Товар" Binding="{Binding ProductName}" Width="*"/>
                            <DataGridTextColumn Header="Остаток" Binding="{Binding CurrentStock}" Width="80"/>
                            <DataGridTextColumn Header="Мин. остаток" Binding="{Binding MinStockLevel}" Width="80"/>
                            <DataGridTextColumn Header="Прогноз (дней)" Binding="{Binding DaysUntilOutDisplay}" Width="120"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>

            <!-- Последние продажи -->
            <Border Background="White" CornerRadius="10" Padding="15">
                <StackPanel>
                    <TextBlock Text="Последние продажи" FontSize="22" FontWeight="Bold" Margin="0 0 0 15"/>
                    <DataGrid ItemsSource="{Binding LastSales}" AutoGenerateColumns="False" Height="300">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding SaleID}"/>
                            <DataGridTextColumn Header="Дата" Binding="{Binding SaleDateTime}"/>
                            <DataGridTextColumn Header="Сумма" Binding="{Binding TotalAmount}"/>
                            <DataGridTextColumn Header="Оплата" Binding="{Binding PaymentMethod}"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>