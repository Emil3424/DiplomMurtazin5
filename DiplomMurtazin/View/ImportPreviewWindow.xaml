<Window x:Class="DiplomMurtazin.View.ImportPreviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Предпросмотр импорта ТОРГ-12"
        Height="500" Width="800"
        WindowStartupLocation="CenterOwner"
        Background="#ecf0f1">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Импорт ТОРГ-12" FontSize="24" FontWeight="Bold" Margin="0,0,0,15"/>

        <Border Grid.Row="1" Background="White" Padding="10" CornerRadius="5" Margin="0,0,0,15">
            <StackPanel>
                <Grid Margin="0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Документ:" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="1" Text="{Binding DocumentNumber}"/>
                </Grid>
                <Grid Margin="0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Дата:" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="1" Text="{Binding DocumentDate, StringFormat=dd.MM.yyyy}"/>
                </Grid>
                <Grid Margin="0,5">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Получатель:" FontWeight="SemiBold"/>
                    <TextBlock Grid.Column="1" Text="{Binding ReceiverName}"/>
                </Grid>
            </StackPanel>
        </Border>

        <DataGrid Grid.Row="2" ItemsSource="{Binding PreviewRows}" AutoGenerateColumns="False" IsReadOnly="True" Background="White">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Наименование" Binding="{Binding ProductName}" Width="*"/>
                <DataGridTextColumn Header="Штрих-код" Binding="{Binding Barcode}" Width="120"/>
                <DataGridTextColumn Header="Кол-во" Binding="{Binding Quantity}" Width="70"/>
                <DataGridTextColumn Header="Цена" Binding="{Binding UnitPrice, StringFormat={}{0:F2}}" Width="90"/>
                <DataGridTextColumn Header="Статус" Binding="{Binding Status}" Width="130">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="{Binding StatusColor}"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,15,0,0">
            <Button Content="Импортировать" Width="140" Height="35" Margin="5" Click="Import_Click" Background="#27ae60" Foreground="White"/>
            <Button Content="Отмена" Width="100" Height="35" Margin="5" Click="Cancel_Click" Background="#95a5a6" Foreground="White"/>
        </StackPanel>
    </Grid>
</Window>